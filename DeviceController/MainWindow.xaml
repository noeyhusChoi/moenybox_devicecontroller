<Window x:Class="DeviceController.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Device Controller"
        Width="1540"
        Height="960"
        MinWidth="1280"
        MinHeight="860">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="280" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="12" Margin="0,0,0,12" BorderBrush="#D8D8D8" BorderThickness="1" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding SummaryText}" />
                    <TextBlock Margin="0,6,0,0" Foreground="#666666" TextWrapping="Wrap" Text="{Binding DiagnosticsText}" />
                    <TextBlock Margin="0,8,0,0" Foreground="#1F1F1F" TextWrapping="Wrap" Text="{Binding LastActionText}" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                    <Button Width="120" Height="34" Margin="0,0,8,0" Content="Refresh All" Command="{Binding RefreshAllCommand}" />
                    <Button Width="120" Height="34" Content="Clear Logs" Command="{Binding ClearLogsCommand}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="480" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Padding="8" BorderBrush="#D8D8D8" BorderThickness="1" CornerRadius="8">
                <DataGrid ItemsSource="{Binding Devices}"
                          SelectedItem="{Binding SelectedDevice}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="DeviceId" Binding="{Binding DeviceId}" Width="140" />
                        <DataGridTextColumn Header="Type" Binding="{Binding DeviceType}" Width="110" />
                        <DataGridTextColumn Header="State" Binding="{Binding ConnectionState}" Width="110" />
                        <DataGridCheckBoxColumn Header="Healthy" Binding="{Binding IsHealthy}" Width="80" />
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Padding="12" Margin="0,0,0,12" BorderBrush="#D8D8D8" BorderThickness="1" CornerRadius="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" FontWeight="SemiBold" Text="Name" />
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedDevice.Name}" />
                            <TextBlock Grid.Row="0" Grid.Column="2" FontWeight="SemiBold" Text="Model" />
                            <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding SelectedDevice.Model}" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,8,0,0" FontWeight="SemiBold" Text="Driver" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,8,0,0" Text="{Binding SelectedDevice.DriverType}" />
                            <TextBlock Grid.Row="1" Grid.Column="2" Margin="0,8,0,0" FontWeight="SemiBold" Text="Vendor" />
                            <TextBlock Grid.Row="1" Grid.Column="3" Margin="0,8,0,0" Text="{Binding SelectedDevice.Vendor}" />

                            <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,8,0,0" FontWeight="SemiBold" Text="Transport" />
                            <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,8,0,0" Text="{Binding SelectedDevice.TransportType}" />
                            <TextBlock Grid.Row="2" Grid.Column="2" Margin="0,8,0,0" FontWeight="SemiBold" Text="Port" />
                            <TextBlock Grid.Row="2" Grid.Column="3" Margin="0,8,0,0" Text="{Binding SelectedDevice.TransportPort}" />

                            <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,8,0,0" FontWeight="SemiBold" Text="Param" />
                            <TextBlock Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3" Margin="0,8,0,0" Text="{Binding SelectedDevice.TransportParam}" TextWrapping="Wrap" />
                        </Grid>
                    </Border>

                    <GroupBox Header="Common Actions" Margin="0,0,0,12">
                        <WrapPanel Margin="8">
                            <Button Width="120" Height="34" Margin="0,0,8,8" Content="Connect" Command="{Binding ConnectCommand}" />
                            <Button Width="120" Height="34" Margin="0,0,8,8" Content="Disconnect" Command="{Binding DisconnectCommand}" />
                            <Button Width="160" Height="34" Margin="0,0,8,8" Content="Refresh Status" Command="{Binding RefreshSelectedStatusCommand}" />
                        </WrapPanel>
                    </GroupBox>

                    <GroupBox Header="Deposit Test" Margin="0,0,0,12">
                        <WrapPanel Margin="8">
                            <Button Width="120" Height="34" Margin="0,0,8,8" Content="Start" Command="{Binding DepositStartCommand}" />
                            <Button Width="120" Height="34" Margin="0,0,8,8" Content="Stop" Command="{Binding DepositStopCommand}" />
                            <Button Width="120" Height="34" Margin="0,0,8,8" Content="Stack" Command="{Binding DepositStackCommand}" />
                            <Button Width="120" Height="34" Margin="0,0,8,8" Content="Return" Command="{Binding DepositReturnCommand}" />
                        </WrapPanel>
                    </GroupBox>

                    <GroupBox Header="ID Scanner Test" Margin="0,0,0,12">
                        <StackPanel Margin="8">
                            <WrapPanel>
                                <Button Width="140" Height="34" Margin="0,0,8,8" Content="Scan Start" Command="{Binding ScannerStartCommand}" />
                                <Button Width="140" Height="34" Margin="0,0,8,8" Content="Scan Stop" Command="{Binding ScannerStopCommand}" />
                                <Button Width="140" Height="34" Margin="0,0,8,8" Content="Save Image" Command="{Binding ScannerSaveImageCommand}" />
                                <Button Width="160" Height="34" Margin="0,0,8,8" Content="Get Scan Status" Command="{Binding ScannerGetScanStatusCommand}" />
                                <Button Width="140" Height="34" Margin="0,0,8,8" Content="Get Device Id" Command="{Binding ScannerGetDeviceIdCommand}" />
                            </WrapPanel>
                            <DockPanel>
                                <TextBox Width="380" Height="32" VerticalContentAlignment="Center" Text="{Binding RunOcrPayloadText, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Width="140" Height="34" Margin="8,0,0,0" Content="Run OCR" Command="{Binding ScannerRunOcrCommand}" />
                            </DockPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Printer Test" Margin="0,0,0,12">
                        <StackPanel Margin="8">
                            <DockPanel Margin="0,0,0,8">
                                <TextBox Width="420" Height="32" VerticalContentAlignment="Center" Text="{Binding PrinterTitleText, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Width="140" Height="34" Margin="8,0,0,0" Content="Print Title" Command="{Binding PrinterPrintTitleCommand}" />
                            </DockPanel>
                            <DockPanel Margin="0,0,0,8">
                                <TextBox Width="420" Height="32" VerticalContentAlignment="Center" Text="{Binding PrinterContentText, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Width="140" Height="34" Margin="8,0,0,0" Content="Print Content" Command="{Binding PrinterPrintContentCommand}" />
                            </DockPanel>
                            <Button Width="120" Height="34" Content="Cut" Command="{Binding PrinterCutCommand}" />
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="QR Test" Margin="0,0,0,12">
                        <WrapPanel Margin="8">
                            <Button Width="140" Height="34" Margin="0,0,8,8" Content="Scan Enable" Command="{Binding QrEnableCommand}" />
                            <Button Width="140" Height="34" Margin="0,0,8,8" Content="Scan Disable" Command="{Binding QrDisableCommand}" />
                        </WrapPanel>
                    </GroupBox>

                    <GroupBox Header="Withdrawal Test" Margin="0,0,0,12">
                        <StackPanel Margin="8">
                            <WrapPanel Margin="0,0,0,8">
                                <Button Width="120" Height="34" Margin="0,0,8,8" Content="Sensor" Command="{Binding WithdrawalSensorCommand}" />
                                <Button Width="120" Height="34" Margin="0,0,8,8" Content="Init" Command="{Binding WithdrawalInitCommand}" />
                            </WrapPanel>
                            <DockPanel Margin="0,0,0,8">
                                <TextBox Width="420" Height="32" VerticalContentAlignment="Center" Text="{Binding WithdrawalEjectValueText, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Width="140" Height="34" Margin="8,0,0,0" Content="Eject" Command="{Binding WithdrawalEjectCommand}" />
                            </DockPanel>
                            <DockPanel>
                                <TextBox Width="420" Height="32" VerticalContentAlignment="Center" Text="{Binding WithdrawalDispenseText, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Width="140" Height="34" Margin="8,0,0,0" Content="Dispense" Command="{Binding WithdrawalDispenseCommand}" />
                            </DockPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Generic Command" Margin="0,0,0,12">
                        <StackPanel Margin="8">
                            <DockPanel Margin="0,0,0,8">
                                <ComboBox Width="260" Height="32" ItemsSource="{Binding AvailableCommands}" SelectedItem="{Binding SelectedGenericCommandName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Width="140" Height="34" Margin="8,0,0,0" Content="Execute" Command="{Binding ExecuteGenericCommand}" />
                            </DockPanel>
                            <TextBox Height="32" VerticalContentAlignment="Center" Text="{Binding GenericPayloadText, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <GroupBox Grid.Row="2" Header="Runtime Log" Margin="0,12,0,0">
            <ListBox ItemsSource="{Binding LogEntries}" Margin="8" />
        </GroupBox>
    </Grid>
</Window>
