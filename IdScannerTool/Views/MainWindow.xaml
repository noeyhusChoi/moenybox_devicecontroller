<ui:FluentWindow
    x:Class="IdScannerTool.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:overlayViewModels="clr-namespace:IdScannerTool.ViewModels.Overlays"
    xmlns:overlayViews="clr-namespace:IdScannerTool.Views.Overlays"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewModels="clr-namespace:IdScannerTool.ViewModels"
    xmlns:views="clr-namespace:IdScannerTool.Views"
    Title="KIOSK ID Scanner"
    Width="1040"
    Height="620"
    MinWidth="920"
    MinHeight="560"
    ExtendsContentIntoTitleBar="True"
    mc:Ignorable="d">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <DataTemplate DataType="{x:Type viewModels:LoadingViewModel}">
            <views:LoadingView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:SerialRegistrationViewModel}">
            <views:SerialRegistrationView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:MainRuntimeViewModel}">
            <views:MainRuntimeView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type overlayViewModels:ProgressOverlayViewModel}">
            <overlayViews:ProgressOverlayView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type overlayViewModels:ConfirmOverlayViewModel}">
            <overlayViews:ConfirmOverlayView />
        </DataTemplate>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Title="신분증스캐너"
            Grid.Row="0"
            ShowClose="True"
            ShowMaximize="True"
            ShowMinimize="True">
            <ui:TitleBar.TrailingContent>
                <ui:ToggleSwitch
                    VerticalAlignment="Center"
                    Content="{Binding IsDarkTheme}"
                    IsChecked="{Binding IsDarkTheme, Mode=TwoWay}" />
            </ui:TitleBar.TrailingContent>
        </ui:TitleBar>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" />

        <Grid
            Grid.RowSpan="2"
            Panel.ZIndex="100"
            Visibility="{Binding IsOverlayVisible, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" Opacity="0.75" />
            <ui:Card
                Padding="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="{DynamicResource ApplicationBackgroundBrush}">

                <ContentControl Content="{Binding CurrentOverlayContent}" />
            </ui:Card>
        </Grid>
    </Grid>
</ui:FluentWindow>
