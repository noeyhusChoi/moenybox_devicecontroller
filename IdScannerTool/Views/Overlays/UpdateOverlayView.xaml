<UserControl
    x:Class="IdScannerTool.Views.Overlays.UpdateOverlayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid MinWidth="360">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            FontSize="20"
            FontWeight="SemiBold"
            Text="{Binding Title}" />

        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="현재 버전" />
            <TextBlock Grid.Row="0" Grid.Column="1" FontWeight="SemiBold" Text="{Binding CurrentVersion}" />

            <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,12,0,0" VerticalAlignment="Center" Text="최신 버전" />
            <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,12,0,0" FontWeight="SemiBold" Text="{Binding LatestVersion}" />
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,20,0,0">
            <TextBlock
                TextWrapping="Wrap"
                Text="{Binding StatusMessage}" />

            <Grid
                Margin="0,16,0,0"
                Visibility="{Binding ShowProgress, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ProgressBar
                    Grid.Column="0"
                    Height="14"
                    Margin="0,0,12,0"
                    Minimum="0"
                    Maximum="100"
                    VerticalAlignment="Center"
                    Value="{Binding ProgressPercent}" />
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    FontWeight="SemiBold"
                    Text="{Binding ProgressPercent, StringFormat={}{0}%}" />
            </Grid>
        </StackPanel>

        <StackPanel
            Grid.Row="3"
            Margin="0,24,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <ui:Button
                Margin="0,0,8,0"
                Appearance="Secondary"
                Command="{Binding CloseCommand}"
                Content="{Binding CloseButtonText}"
                Visibility="{Binding CanClose, Converter={StaticResource BoolToVisibilityConverter}}" />
            <ui:Button
                Margin="0,0,8,0"
                Appearance="Secondary"
                Command="{Binding CancelCommand}"
                Content="취소"
                Visibility="{Binding CanCancel, Converter={StaticResource BoolToVisibilityConverter}}" />
            <ui:Button
                Margin="0,0,8,0"
                Appearance="Primary"
                Command="{Binding UpdateCommand}"
                Content="{Binding UpdateButtonText}"
                Visibility="{Binding CanUpdate, Converter={StaticResource BoolToVisibilityConverter}}" />
            <ui:Button
                Appearance="Primary"
                Command="{Binding RestartCommand}"
                Content="지금 재시작"
                Visibility="{Binding CanRestart, Converter={StaticResource BoolToVisibilityConverter}}" />
        </StackPanel>
    </Grid>
</UserControl>
