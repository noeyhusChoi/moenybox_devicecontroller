<UserControl
    x:Class="KIOSK.Modules.GTF.Views.GtfIdScanConsentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="1820"
    d:DesignWidth="1080"
    Background="{StaticResource SecondaryBrush}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="ScrollBar">
            <!--  세로는 폭 8, 가로는 높이 8  -->
            <Setter Property="Width" Value="8" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track">
                                <!--  버튼 영역 제거  -->
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Visibility="Collapsed" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Visibility="Collapsed" />
                                </Track.IncreaseRepeatButton>

                                <!--  Thumb 정의  -->
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <!--  기본 모양  -->
                                                <Setter Property="Width" Value="6" />
                                                <Setter Property="Background" Value="DarkGray" />
                                                <Setter Property="Opacity" Value="0.5" />

                                                <!--  둥근 모서리를 Border로 구현  -->
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border
                                                                Margin="1"
                                                                Background="{TemplateBinding Background}"
                                                                CornerRadius="3" />
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>

                                                <!--  Hover 효과  -->
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Opacity" Value="0.9" />
                                                        <Setter Property="Background" Value="Gray" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>

                        <!--  방향 보정  -->
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="true" />
                            </Trigger>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Height" Value="2" />
                                <Setter Property="Width" Value="Auto" />
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="false" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Width="1000" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <ListBox
            Grid.Row="1"
            Padding="16"
            Background="Transparent"
            BorderThickness="0"
            Focusable="False"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ScrollViewer.VerticalScrollBarVisibility="Disabled"
            SelectionMode="Single">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,8,0,0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Focusable" Value="False" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <StackPanel>

                                    <!--  ▼ HEADER (ToggleButton)  -->
                                    <ToggleButton
                                        x:Name="HeaderBtn"
                                        Height="50"
                                        Padding="16,12"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Content="{TemplateBinding Content}"
                                        Cursor="Hand"
                                        IsChecked="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                        OverridesDefaultStyle="True">

                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border
                                                    x:Name="HeaderBG"
                                                    Background="#111827"
                                                    CornerRadius="6">

                                                    <Grid Margin="10">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="8" />
                                                            <ColumnDefinition Width="*" />
                                                        </Grid.ColumnDefinitions>

                                                        <Path
                                                            x:Name="Arrow"
                                                            Width="10"
                                                            Height="12"
                                                            Data="M 0 0 L 6 6 L 0 12"
                                                            Fill="White"
                                                            RenderTransformOrigin="0.5,0.5">
                                                            <Path.RenderTransform>
                                                                <RotateTransform Angle="0" />
                                                            </Path.RenderTransform>
                                                        </Path>

                                                        <ContentPresenter
                                                            Grid.Column="2"
                                                            VerticalAlignment="Center"
                                                            TextElement.FontSize="18"
                                                            TextElement.FontWeight="SemiBold"
                                                            TextElement.Foreground="White" />
                                                    </Grid>
                                                </Border>

                                                <!--  ToggleButton Trigger  -->
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="HeaderBG" Property="Background" Value="Black" />
                                                        <!--<Setter TargetName="Arrow" Property="RenderTransform.(RotateTransform.Angle)" Value="90"/>-->
                                                    </Trigger>

                                                    <Trigger Property="IsChecked" Value="False">
                                                        <Setter TargetName="HeaderBG" Property="Background" Value="#111827" />
                                                        <!--<Setter TargetName="Arrow" Property="RenderTransform.(RotateTransform.Angle)" Value="0"/>-->
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>


                                    <!--  ▼ 내용 영역 (본문)  -->
                                    <Border
                                        x:Name="Body"
                                        Height="500"
                                        Margin="0,6,0,0"
                                        Padding="10"
                                        Background="#F9FAFB"
                                        BorderBrush="#E5E7EB"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Visibility="Collapsed">
                                        <ScrollViewer
                                            MaxHeight="450"
                                            Cursor=""
                                            HorizontalScrollBarVisibility="Disabled"
                                            VerticalScrollBarVisibility="Auto">
                                            <Image Source="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Tag}" Stretch="Uniform" />
                                        </ScrollViewer>
                                    </Border>
                                </StackPanel>
                                <!--  ListBoxItem Trigger  -->
                                <ControlTemplate.Triggers>
                                    <Trigger SourceName="HeaderBtn" Property="IsChecked" Value="True">
                                        <Setter TargetName="Body" Property="Visibility" Value="Visible" />
                                    </Trigger>

                                    <Trigger SourceName="HeaderBtn" Property="IsChecked" Value="False">
                                        <Setter TargetName="Body" Property="Visibility" Value="Collapsed" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBoxItem
                Content="(필수) 전자금융거래 이용약관"
                IsSelected="True"
                Tag="../../../Assets/Image/Terms/Terms_ko-KR.png" />
            <ListBoxItem Content="(필수) 개인정보 수집·이용 동의" Tag="../../../Assets/Image/Terms/Terms_ko-KR.png" />
            <ListBoxItem Content="(선택) 마케팅 정보 수신 동의" Tag="../../../Assets/Image/Terms/Terms_ko-KR.png" />
        </ListBox>

        <Grid Grid.Row="2" Margin="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                Width="200"
                Height="80"
                Command="{Binding MainCommand}"
                Content="취소" />

            <Button
                Grid.Column="2"
                Width="200"
                Height="80"
                Command="{Binding NextCommand}"
                Content="서비스 동의" />
        </Grid>
    </Grid>
</UserControl>
