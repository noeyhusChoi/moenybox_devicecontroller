<UserControl
    x:Class="KIOSK.Views.Exchange.Popup.ExchangePopupTermsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:KIOSK.Views.Exchange.Popup"
    xmlns:lx="clr-namespace:Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:KIOSK.ViewModels.Exchange.Popup"
    Width="1540"
    Height="780"
    d:DataContext="{d:DesignInstance Type=viewModels:ExchangePopupTermsViewModel,
                                     IsDesignTimeCreatable=False}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <!--  전역 ScrollBar 스타일  -->
        <Style TargetType="ScrollBar">
            <!--  세로는 폭 8, 가로는 높이 8  -->
            <Setter Property="Width" Value="8" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track">
                                <!--  버튼 영역 제거  -->
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Visibility="Collapsed" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Visibility="Collapsed" />
                                </Track.IncreaseRepeatButton>

                                <!--  Thumb 정의  -->
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <!--  기본 모양  -->
                                                <Setter Property="Width" Value="6" />
                                                <Setter Property="Background" Value="DarkGray" />
                                                <Setter Property="Opacity" Value="0.5" />

                                                <!--  둥근 모서리를 Border로 구현  -->
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border
                                                                Margin="1"
                                                                Background="{TemplateBinding Background}"
                                                                CornerRadius="3" />
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>

                                                <!--  Hover 효과  -->
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Opacity" Value="0.9" />
                                                        <Setter Property="Background" Value="Gray" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>

                        <!--  방향 보정  -->
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="true" />
                            </Trigger>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Height" Value="2" />
                                <Setter Property="Width" Value="Auto" />
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="false" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border
        Background="White"
        BorderBrush="Gray"
        BorderThickness="1"
        CornerRadius="30"
        SnapsToDevicePixels="True">
        <Grid Margin="70,0,70,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="78" />
                <RowDefinition Height="103" />
                <RowDefinition Height="3*" />
                <RowDefinition Height="42" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="36" />
            </Grid.RowDefinitions>
            <TextBlock
                Grid.Row="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontFamily="{StaticResource Pretendard}"
                FontSize="{StaticResource FontSize_3XL}"
                FontWeight="{StaticResource FontWeight_M}"
                Text="{lx:Loc ExchangeTermsPopup.Title}" />
            <ScrollViewer
                Grid.Row="2"
                HorizontalScrollBarVisibility="Disabled"
                IsManipulationEnabled="True"
                PanningMode="VerticalOnly"
                VerticalScrollBarVisibility="Visible">
                <Image
                    x:Name="image"
                    Margin="150,0,150,0"
                    Source="{Binding Source}"
                    Stretch="UniformToFill" />
            </ScrollViewer>
            <Button
                Grid.Row="4"
                Width="430"
                Height="126"
                Background="{StaticResource PrimaryBrush}"
                BorderBrush="{StaticResource PrimaryBrush}"
                Command="{Binding CloseCommand}"
                Content="{lx:Loc Common.Ok}"
                FontFamily="{StaticResource Pretendard}"
                FontSize="{StaticResource FontSize_L}"
                Foreground="White">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</UserControl>
