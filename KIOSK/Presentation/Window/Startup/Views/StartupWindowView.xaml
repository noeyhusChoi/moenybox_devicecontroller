<Window
    x:Class="KIOSK.Presentation.Shell.Window.Startup.Views.StartupWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:KIOSK.Presentation.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:KIOSK.Presentation.Shell.Window.Startup.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="StartupWindowView"
    Width="1080"
    Height="1920"
    Background="#1F2EF8"
    WindowStartupLocation="CenterScreen"
    WindowState="Maximized"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="13*" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontFamily="{StaticResource Pretendard}"
            FontSize="{StaticResource FontSize_L}"
            FontWeight="{StaticResource FontWeight_M}"
            Foreground="{StaticResource WhiteBrush}"
            Text="{Binding Message}" />
        <controls:LottiePlayerControl
            Grid.Row="1"
            Width="800"
            Height="688"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            AutoPlay="True"
            SourcePath="{Binding LottieSourcePath}" />
    </Grid>
</Window>
