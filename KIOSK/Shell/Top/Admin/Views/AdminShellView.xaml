<UserControl
    x:Class="KIOSK.Shell.Top.Admin.Views.AdminShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:KIOSK.Shell.Top.Admin.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <!--  Content 중앙 콘텐츠 영역  -->
        <ContentControl
            x:Name="MainContent"
            Margin="0,0,0,100"
            Background="#2222F0"
            Content="{Binding CurrentSubShell}" />

        <!--  Popup  -->
        <!--<ContentControl Panel.ZIndex="1000" Content="{Binding PopupViewModel}">
     <ContentControl.Style>
         <Style TargetType="ContentControl">
             <Setter Property="Opacity" Value="0" />
             <Setter Property="IsHitTestVisible" Value="False" />
             <Style.Triggers>
                 <DataTrigger Binding="{Binding IsPopupOpen}" Value="True">
                     <Setter Property="Opacity" Value="1" />
                     <Setter Property="IsHitTestVisible" Value="True" />
                 </DataTrigger>
             </Style.Triggers>
         </Style>
     </ContentControl.Style>

 </ContentControl>-->
    </Grid>
</UserControl>
