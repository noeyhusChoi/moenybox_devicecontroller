<UserControl
    x:Class="Kiosk.Views.Steps.ScanCompletedStepView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="ResultHeadlineStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="60" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="ResultDescriptionStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="40" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="ResultNoticeStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="40" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Foreground" Value="{DynamicResource AccentEmphasisForegroundBrush}" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessVisualStyle" TargetType="Grid">
            <Setter Property="Visibility" Value="Visible" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FailureVisualStyle" TargetType="Grid">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="224" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Style="{StaticResource ExchangeStepTitleStyle}"
            Text="{Binding Title}" />

        <Grid
            Grid.Row="1"
            Width="936"
            Focusable="False"
            KeyboardNavigation.IsTabStop="False">
            <StackPanel Margin="0,258,0,0" HorizontalAlignment="Center">
                <Grid
                    Width="182"
                    Height="182"
                    Style="{StaticResource SuccessVisualStyle}">
                    <Ellipse Fill="{DynamicResource AccentBackgroundBrush}" />
                    <Path
                        Width="58"
                        Height="42"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Data="M 4,21 L 22,39 L 54,7"
                        Stroke="White"
                        StrokeEndLineCap="Round"
                        StrokeLineJoin="Round"
                        StrokeThickness="8" />
                </Grid>

                <Grid
                    Width="182"
                    Height="182"
                    Style="{StaticResource FailureVisualStyle}">
                    <Ellipse Fill="#F14B00" />
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border
                            Width="14"
                            Height="73"
                            Background="White"
                            CornerRadius="7" />
                        <Ellipse
                            Width="14"
                            Height="14"
                            Margin="0,10,0,0"
                            Fill="White" />
                    </StackPanel>
                </Grid>

                <TextBlock
                    Margin="0,85,0,0"
                    Style="{StaticResource ResultHeadlineStyle}"
                    Text="{Binding Headline}" />
                <TextBlock
                    Margin="0,24,0,0"
                    Style="{StaticResource ResultDescriptionStyle}"
                    Text="{Binding Description}" />
                <TextBlock
                    Margin="0,24,0,0"
                    Style="{StaticResource ResultNoticeStyle}"
                    Text="{Binding NoticeText}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
