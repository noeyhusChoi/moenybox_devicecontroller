<UserControl
    x:Class="Kiosk.Views.HomeShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Kiosk.ViewModels"
    xmlns:views="clr-namespace:Kiosk.Views"
    mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewModels:HomeScreenViewModel}">
            <views:HomeScreenView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:HomeLanguageSelectionViewModel}">
            <views:HomeLanguageSelectionView />
        </DataTemplate>
    </UserControl.Resources>

    <ContentControl Content="{Binding CurrentScreenViewModel}" Focusable="False" />
</UserControl>
